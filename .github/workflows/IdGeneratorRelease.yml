name: Build and Release for IdGenerator

on:
  workflow_dispatch:
  push:
    paths:
      - 'IdGenerator/**'
      - 'IdGenerator.Core/**'
      - 'IdGenerator.Cli/**'
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Set release version
      run: |
        echo "RELEASE_VERSION=1.0.0.${{ github.run_number }}" >> $env:GITHUB_ENV

    - name: Build core library
      run: dotnet build IdGenerator.Core/IdGenerator.Core.csproj --configuration Release -p:Version=${{ env.RELEASE_VERSION }} -p:AssemblyVersion=${{ env.RELEASE_VERSION }} -p:FileVersion=${{ env.RELEASE_VERSION }}

    - name: Build WinForms app
      run: dotnet build IdGenerator/IdGenerator.csproj --configuration Release -p:Version=${{ env.RELEASE_VERSION }} -p:AssemblyVersion=${{ env.RELEASE_VERSION }} -p:FileVersion=${{ env.RELEASE_VERSION }}

    - name: Publish WinForms app
      run: dotnet publish IdGenerator/IdGenerator.csproj --configuration Release --output ./publish -p:Version=${{ env.RELEASE_VERSION }} -p:AssemblyVersion=${{ env.RELEASE_VERSION }} -p:FileVersion=${{ env.RELEASE_VERSION }}

    - name: Build CLI tool
      run: dotnet build IdGenerator.Cli/IdGenerator.Cli.csproj --configuration Release -p:Version=${{ env.RELEASE_VERSION }} -p:PackageVersion=${{ env.RELEASE_VERSION }} -p:AssemblyVersion=${{ env.RELEASE_VERSION }} -p:FileVersion=${{ env.RELEASE_VERSION }}

    - name: Pack CLI tool
      run: dotnet pack IdGenerator.Cli/IdGenerator.Cli.csproj --configuration Release --output ./tool-pack -p:Version=${{ env.RELEASE_VERSION }} -p:PackageVersion=${{ env.RELEASE_VERSION }} -p:AssemblyVersion=${{ env.RELEASE_VERSION }} -p:FileVersion=${{ env.RELEASE_VERSION }}

    - name: Zip published files
      run: |
        Compress-Archive -Path ./publish/* -DestinationPath ./IdGenerator.zip
        Compress-Archive -Path ./tool-pack/* -DestinationPath ./IdGenerator.Cli.zip

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ github.run_number }}
        name: "Release ${{ github.run_number }}"
        body: "Automated release. Includes WinForms IdGenerator and idgen CLI tool package."
        files: |
          ./IdGenerator.zip
          ./IdGenerator.Cli.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
